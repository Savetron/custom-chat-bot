// Authentication middleware removed as per project requirements.
// This file can be deleted if not needed for other middleware purposes.
export const config = {
  matcher: [], // No routes need middleware anymore
};

export default function middleware() {
  // No-op middleware
}
